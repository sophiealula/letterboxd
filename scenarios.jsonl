{"name": "homepage-loads", "description": "Landing page loads with core elements", "given": "User visits the homepage", "when": "Page loads", "then": "Headline, CTA button, and product mockup are visible", "validates": ["index.html", "landing page content"]}
{"name": "setup-form-loads", "description": "Setup page has all form elements", "given": "User visits /setup.html", "when": "Page loads", "then": "Username input, 12 streaming services, and check button are present", "validates": ["setup.html", "form elements"]}
{"name": "watchlist-api-works", "description": "Watchlist API fetches Letterboxd data", "given": "Valid Letterboxd username", "when": "GET /api/watchlist?username=X&page=1", "then": "Returns HTML containing Letterboxd watchlist content", "validates": ["netlify/functions/watchlist.js", "Letterboxd scraping"]}
{"name": "justwatch-api-works", "description": "JustWatch API proxies GraphQL requests", "given": "Valid GraphQL query", "when": "POST /api/justwatch with JSON body", "then": "Returns JustWatch GraphQL response", "validates": ["netlify/functions/justwatch.js", "JustWatch integration"]}
{"name": "no-app-promotion", "description": "Pages don't promote app downloads", "given": "User views any page", "when": "Checking page content", "then": "No 'download app' or 'install app' language; 'no app' messaging present", "validates": ["index.html", "setup.html", "web-only messaging"]}
{"name": "analytics-setup", "description": "GoatCounter analytics properly configured", "given": "Deployed site", "when": "Loading any page", "then": "GoatCounter script present with correct site code, JS loads, dashboard URL documented", "validates": ["index.html", "setup.html", "GoatCounter integration"]}
